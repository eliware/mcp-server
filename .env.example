# Required for protected HTTP/HTTPS deployments.
MCP_TOKEN=replace-me
MCP_HTTP_PORT=1234
# Set MCP_STDIO=true for local stdio mode.
MCP_STDIO=false
# HTTPS: mount cert files and set these in the entrypoint/application.
TLS_KEY_FILE=/run/secrets/tls.key
TLS_CERT_FILE=/run/secrets/tls.crt
